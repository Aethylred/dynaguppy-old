# This node defines your dynaguppy puppetmaster

# Either use 'puppet' as an alias in /etc/hosts or rename this node with
# the FQDN of the puppetmaster server in this installation

node $dynaguppy_puppetmaster {

}